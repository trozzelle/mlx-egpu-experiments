#include "vram_smoke_asset.h"

// The loader is inline so amdev_session.cpp remains self-contained in the
// runner's established source closure. Keep this translation unit as the
// dedicated asset-loader compilation anchor for builds that enumerate assets.
