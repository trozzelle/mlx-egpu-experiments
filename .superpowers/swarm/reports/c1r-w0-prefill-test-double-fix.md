# C1R W0 Prefill Test-Double Fix

Updated the two CLI-focused `fake_prefill_prompt_prefix` doubles in `tests/native_r9700/test_prefill.py` to accept keyword-only `producer_kind` and assert `_CPU_REFERENCE_PRODUCER_KIND`.

No commands were run, per assignment constraint.
